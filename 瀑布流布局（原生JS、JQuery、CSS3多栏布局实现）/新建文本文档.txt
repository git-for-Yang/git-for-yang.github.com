瀑布流布局：用绝对定位固定图片位置，图片等宽不等高，滚动滚轮能自动加载。
区别：1、CSS3不需要计算列数，浏览器会自动计算，只需要设置列宽，（column-width），性能高；
         JS需要计算，且图片定位。
      2、 列与列之间的宽度随着浏览器窗口大小进行改变，用户体验不好；
      3、JS方式图片排序按照从左到右顺序规范显示，CSS3方式会打乱图片显示顺序，即按照垂直顺序排序；
      4、图片加载还是要依靠JS实现。